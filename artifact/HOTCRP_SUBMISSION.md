# MICRO 2026 AE Submission Fields

This file is copy-ready except for the DOI URL and measured clean-room resource
figures. Do not mark the HotCRP submission ready until every item in the final
checklist is complete.

Current badge scope is **Artifact Available** and **Artifact Functional**.
Do not request **Results Reproduced** unless both the sparse-SAES contract and
the missing FSDR LSH collateral are recovered and the machine-readable claim
status is restored with real aggregate evidence.

## Blocking Abstract Correction

The current HotCRP draft screenshot says the design is a fabricated chip. The
accepted manuscript says it is a post-layout design. Replace the fabricated-chip
wording before submission. The artifact contains no silicon measurement and
must not be used to support a fabrication claim.

## DOI URL

Pending the final Zenodo upload. This field must contain the DOI URL returned by
Zenodo, and that DOI must resolve to the exact release validated by
`scripts/check_release.py`.

## Key Results to be Reproduced

1. **RTL functionality.** Validate the Chisel RTL, SystemVerilog emission,
   Verilator lint, and representative VCD with `bash scripts/run_ae.sh rtl`.

The public iFlow/ASAP7 workflow passed its pinned clean-worktree, container,
command, and collateral-hash dry-run but is `NOT_CLAIMED_RESOURCE_LIMIT` because
the host resource gate was not met. DeepScale formula/table tests remain
Functional; no 28 nm estimate is reported without routed ASAP7 input.

Table 1 and Tables 2--3 must not be listed as reproduced in the present
submission. Real sparse-SAES probes exceed the quality tolerance and do not
reproduce the paper's L1 rates. Six FSDR-only pilots also fail the Guided Rate
targets; the public RTL contains an all-zero LSH projection ROM rather than the
hyperplanes needed to reproduce the paper's guided set. Exact discrete Top-1
evidence is retained as a diagnostic but is not claimed independently because
its denominator depends on that unrecoverable guided set. The closer
dense-interpolation diagnostic retains every Gaussian and is explicitly
rejected by claim runs.

All dataset-level claims use the ordered protocol recovered from the committed
upstream evaluation indices. Source index hashes, prepared tree hashes, and
actual result selections are recorded in the final archive. The representative
LPDDR5 Ramulator and DRAMPower trace is Functional evidence only and is not a
paper-result claim.

Figure 9 and Table 4 TSMC 28 nm absolute PPA values are comparison targets and
are not claimed as regenerated by the public predictive-PDK flow.

Figure 8 is outside the reproduced claim because the artifact has no real Orin
NX evidence. DL3DV rows are outside the claim because current gated access is
unavailable. Figure 11 and Figures 13-16 require the complete nine-pair matrix
and are therefore also outside the claim.

## Hardware Dependencies

- Quick strict inference: NVIDIA CUDA GPU with at least 8 GB VRAM, x86-64
  host, 16 GB RAM, and 20 GB free disk. It uses the generated synthetic fixture
  and is Functional evidence only.
- CPU smoke validation: x86-64 CPU and 8 GB RAM for the CLI, result schema,
  DeepScale, and applicable RTL tools.
- Full quality evaluation: NVIDIA CUDA GPU with at least 24 GB VRAM, 32 GB RAM,
  and sufficient disk for the selected datasets and checkpoints.
- Optional Figure 8 measurement: Jetson Orin NX 16 GB in MAXN mode. This is not
  part of the current claim.
- RTL validation: x86-64 Linux host with 16 GB RAM.
- Physical proxy: x86-64 Linux host with 128 GB RAM recommended and 100 GB free
  disk. No FPGA or ASIC hardware is required.

## Software Dependencies

- Ubuntu 22.04 and Python 3.10 for model evaluation.
- The pinned TranSplat/MVSplat and DepthSplat environments supplied with the
  artifact. NVIDIA driver and CUDA versions must satisfy the selected profile.
- JDK 11+, sbt 1.9+, Chisel 6.6, and Verilator 5+ for RTL validation.
- Pinned iFlow commit `04b4d98` and its ASAP7 platform for physical design.
- Ramulator 2 and DRAMPower for DRAM timing and energy validation.
- No proprietary tool is required for the public workflow. Commercial TSMC
  process and memory-compiler collateral used for the paper cannot be
  redistributed and is not required for the public proxy claims.

## Data Dependencies

The diagnostic experiments use Re10K and ACID evaluation data plus official
pretrained checkpoints for the three models. DL3DV download and conversion
remain available as an unclaimed gated workflow. `artifact/manifests/` records the
source, license status, expected SHA256, and model/dataset mapping. The final
archive includes the ordered evaluation indices but does not duplicate full
datasets unless redistribution is permitted. The public LPDDR5 workflow
includes a small representative functional trace that is not a paper workload.

## Submission PDF

Upload the accepted paper with `artifact/appendix.tex` appended. Verify that the
appendix is at most two pages and that its DOI matches the DOI URL field.
